package art.arcane.iris.generation.hydrology;

public enum RiverCourseType {
    SURFACE,
    UNDERGROUND,
    DEEP_FLUID,
    SURFACE_POOL,
    // A coastal grotto the sea opens into on its own: no drainage source, no river outlet.
    SEA_CAVE
}
