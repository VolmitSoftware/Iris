package art.arcane.iris.generation.image;

import art.arcane.volmlib.util.documentation.Description;

@Description("The generation input controlled by an image map")
public enum IrisImageMapApplication {
    TERRAIN_HEIGHT,
    BIOME,
    REGION,
    SURFACE_BLOCK,
    MASK,
    CUSTOM
}
